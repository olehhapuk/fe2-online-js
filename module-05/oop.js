function addNumbers(a, b) {
  let total = a + b;
  return total;
}

const a = addNumbers(5, 10);
const b = addNumbers(9, 10);

console.log(a, b);

const total = 5 + 10;

const total = addNumbers(5, 10);
